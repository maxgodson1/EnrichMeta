% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/keggenrich.R
\name{keggenrich}
\alias{keggenrich}
\title{Perform KEGG metabolite enrichment with caching and progress tracking}
\usage{
keggenrich(
  KEGGid,
  species,
  p.adjust.method = "BH",
  use_cache = TRUE,
  cache_dir = tempdir()
)
}
\arguments{
\item{KEGGid}{A character vector of KEGG compound IDs (e.g., "C00160")}

\item{species}{A character string, KEGG species code (e.g., "hsa", "eco")}

\item{p.adjust.method}{Adjustment method for multiple testing, default is "BH"}

\item{use_cache}{Logical, whether to use cached pathway data, default TRUE}

\item{cache_dir}{Directory to store cache files, default is tempdir()}
}
\value{
A data.frame with enrichment results containing the following columns:
\itemize{
  \item pathwayID: KEGG pathway ID
  \item Description: Pathway description
  \item MetaRatio: Ratio of metabolites in the pathway (e.g., "5/20")
  \item BgRatio: Background ratio (e.g., "15/1000")
  \item pvalue: Raw p-value
  \item EnrichmentRatio: Enrichment ratio
  \item keggID: KEGG IDs of metabolites in the pathway
  \item Count: Number of metabolites in the pathway
  \item p.adjust: Adjusted p-value
}
}
\description{
This function performs KEGG pathway enrichment analysis for metabolites. 
It supports caching of pathway data to speed up repeated analyses. 
The function includes progress tracking for long-running operations.
}
\details{
The enrichment analysis is performed using the hypergeometric test. 
Pathway data is fetched from KEGG using the KEGGREST package. 
Cached data is stored in RDS format for faster subsequent access.
}
\examples{
\dontrun{
# Example usage
results <- keggenrich(c("C00031", "C00022", "C00041", "C00024"), species = "hsa")
head(results)
}
}
\seealso{
\code{\link[KEGGREST]{keggList}}, \code{\link[KEGGREST]{keggGet}}
}
\author{
Botao Zhang
}
\keyword{enrichment}
\keyword{kegg}
