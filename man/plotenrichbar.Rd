% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotenrichbar.R
\name{plotenrichbar}
\alias{plotenrichbar}
\title{Draw a Bar Plot for KEGG Enrichment Results}
\usage{
plotenrichbar(
  results,
  top = 25,
  title = "Overview of Enriched Metabolite Sets",
  low_color = "#FF0000",
  high_color = "#FFFF00",
  bar_width = 0.8,
  base_size = 12,
  wrap_width = 40,
  show_ratio = TRUE,
  color_limits = c(0, 0.2),
  legend_position = "right"
)
}
\arguments{
\item{results}{A data frame containing KEGG enrichment results as returned by `enrichkegg()`.
Must contain columns: `Description`, `EnrichmentRatio`, `pvalue`, `p.adjust`.}

\item{top}{Number of top enriched pathways to display (default = 25)}

\item{title}{Plot title (default = "Overview of Enriched Metabolite Sets")}

\item{low_color}{Color for the most significant p-values (default = "#FF0000" - red)}

\item{high_color}{Color for the least significant p-values (default = "#FFFF00" - yellow)}

\item{bar_width}{Width of the bars (default = 0.8)}

\item{base_size}{Base font size for the plot (default = 12)}

\item{wrap_width}{Number of characters per line for wrapping pathway descriptions (default = 40)}

\item{show_ratio}{Logical indicating whether to display enrichment ratio values on the bars (default = TRUE)}

\item{color_limits}{Range of p-values for the color scale (default = c(0, 0.2))}

\item{legend_position}{Position of the legend ("none", "left", "right", "bottom", "top", or two-element numeric vector; default = "right")}
}
\value{
A ggplot2 object representing the enrichment bar plot.
}
\description{
Creates a horizontal bar plot to visualize KEGG enrichment results, with bars colored by p-value significance.
}
\details{
This function creates a horizontal bar plot where:
\itemize{
  \item Each bar represents a KEGG pathway
  \item Bar length corresponds to the fold enrichment
  \item Bar color indicates the p-value (red = significant, yellow = non-significant)
  \item Pathways are ordered by adjusted p-value (most significant at the top)
}
}
\examples{
\dontrun{
# Assuming 'enrich_results' is output from keggenrich()

# Basic plot
plotenrichbar(enrich_results)

# Customized plot
plotenrichbar(
  results = enrich_results,
  top = 15,
  title = "Top Enriched Pathways",
  low_color = "darkred",
  high_color = "lightyellow",
  bar_width = 0.7,
  base_size = 14,
  wrap_width = 30,
  show_ratio = FALSE,
  color_limits = c(0, 0.1),
  legend_position = "bottom"
)
}

}
