% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotenrichdot.R
\name{plotenrichdot}
\alias{plotenrichdot}
\title{Draw a Dot Plot for KEGG Enrichment Results}
\usage{
plotenrichdot(
  results,
  top = 25,
  title = "Enriched Metabolic Pathways",
  low_color = "#FF0000",
  high_color = "#FFFF00",
  point_size_range = c(3, 8),
  point_alpha = 0.8,
  base_size = 12,
  wrap_width = 40,
  color_limits = c(0, 0.15),
  legend_position = "right",
  show_grid = TRUE,
  x_expansion = c(0, 0.15),
  padding_right = 0.3
)
}
\arguments{
\item{results}{A data frame containing KEGG enrichment results as returned by `enrichkegg()`}

\item{top}{Number of top pathways to display (default = 25)}

\item{title}{Plot title (default = "Enriched Metabolic Pathways")}

\item{low_color}{Color for significant p-values (default = "#FF0000" - red)}

\item{high_color}{Color for non-significant p-values (default = "#FFFF00" - yellow)}

\item{point_size_range}{Range of point sizes (default = c(3, 8))}

\item{point_alpha}{Point transparency (default = 0.8)}

\item{base_size}{Base font size (default = 12)}

\item{wrap_width}{Number of characters per line for wrapping descriptions (default = 40)}

\item{color_limits}{Color scale limits (default = c(0, 0.15))}

\item{legend_position}{Legend position ("none", "left", "right", "bottom", "top"; default = "right")}

\item{show_grid}{Show horizontal grid lines? (default = TRUE)}

\item{x_expansion}{Multiplicative expansion for x-axis (default = c(0, 0.15))}

\item{padding_right}{Additional right padding for points (default = 0.3)}
}
\value{
A ggplot2 object
}
\description{
Creates a dot plot to visualize KEGG enrichment results, with point size representing enrichment ratio and color representing p-value.
}
\details{
This function creates a dot plot where:
\itemize{
  \item Point position (x-axis) represents -log10(p-value)
  \item Point size represents fold enrichment
  \item Point color indicates p-value significance
  \item Pathways are ordered by adjusted p-value
}
}
\examples{
\dontrun{
# Basic dot plot
plotenrichdot(enrich_results)

# Customized dot plot
plotenrichdot(
  results = enrich_results,
  top = 15,
  title = "Top Metabolic Pathways",
  low_color = "darkred",
  point_size_range = c(4, 10),
  base_size = 14
)
}

}
